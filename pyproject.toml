[project]
name = "ci-cd-ai-service"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Gulshan Yadav", email = "GYadav@cssoftsolutions.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
    "pytest>=9.1.1",
    "uvicorn>=0.53.0",
]

[project.scripts]
ci-cd-ai-service = "ci_cd_ai_service:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "ruff>=0.16.8",
]
